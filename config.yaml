server:
  grpc_port: 8080
  http_port: 8090

database:
  uri: mongodb://localhost:27017/
  db: mydb

redis:
  host: localhost
  port: 6379
  password: ""

rabbitmq:
  host: localhost
  port: 5672
  user: guest
  password: guest

user_service:
  jwt_secret: secret
  token_expiration: 24h

post_service:
  max_post_length: 1000

comment_service:
  max_comment_length: 256

logging:
  level: info
  format: json